
// Calculates the length of a String.
// Remember, length counts every character in a String. This includes special characters (period) and white spaces (space).
const myStr = "This string has 30 characters.";

const myStr_length = myStr.length;

console.log(myStr_length); // Output | 30